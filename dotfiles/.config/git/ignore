# Global list of things to ignore in every Git repo.

# VS Code workspace.
/.vscode/

# macOS Finder cached info.
.DS_Store

# IntelliJ IDEA workspace.
/.idea/

# VirtualEnvs
/.venv/

# Vim per-project languageserver config.
# I think coc.nvim specific.
/.vim/

# Bazel build dependencies (no leading slash as you can multiple bazel projects in one repo).
.bazel/
bazel-out

# Any dotdir in the root that has a secrets subdir.
/.*/secrets/

# Temp cache of Bazel queries.
.fzf-bazel-query-cache
