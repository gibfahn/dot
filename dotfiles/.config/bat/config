# Set the theme to "TwoDark"
--theme="TwoDark"

# No fancy styles. Use `--style="changes"` to see git gutter.
--style="plain"

# Use italic text on the terminal (not supported on all terminals)
--italic-text=always

# TODO(gib): upstream these to bat.
# Use Python highlighting for Bazel files.
--map-syntax "*.bazel:Python"
--map-syntax "WORKSPACE:Python"
--map-syntax ".bazelrc:bash"
--map-syntax "*.pcl:Groovy"
